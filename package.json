{
  "name": "intsort",
  "version": "0.0.1",
  "description": "An efficient integer sort that outperforms v8 Array#sort.",
  "main": "./lib/index.js",
  "scripts": {
    "test": "mocha --require babel-register",
    "build": "mkdir -p ./lib && cat ./src/radix.js | babel > ./lib/index.js"
  },
  "keywords": [
    "sort",
    "integer",
    "array",
    "int"
  ],
  "author": "synchronous",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-babili": "0.0.4",
    "babel-preset-latest": "^6.16.0",
    "babel-preset-stage-0": "^6.16.0",
    "babel-register": "^6.16.3",
    "chai": "^3.5.0",
    "mocha": "^3.1.0"
  }
}
